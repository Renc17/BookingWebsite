import { GetMainRentalPicPipe } from './get-main-rental-pic.pipe';

describe('GetMainRentalPicPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMainRentalPicPipe();
    expect(pipe).toBeTruthy();
  });
});
